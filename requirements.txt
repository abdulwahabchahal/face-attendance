# ── Core API ──────────────────────────────────────────────────────────────────
fastapi>=0.110.0,<0.115.0
uvicorn[standard]>=0.29.0,<0.31.0
python-multipart>=0.0.9          # Required for Form/File uploads
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0,<2.1.0
aiosqlite>=0.19.0                # Async SQLite driver

# ── ML / Computer Vision ──────────────────────────────────────────────────────
torch>=2.2.0,<2.4.0
torchvision>=0.17.0,<0.19.0
facenet-pytorch==2.6.0           # MTCNN + InceptionResnetV1
opencv-python-headless>=4.8.0   # headless = no GUI deps
Pillow>=10.0.0,<11.0.0
imgaug>=0.4.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0

# ── Scheduler ─────────────────────────────────────────────────────────────────
apscheduler>=3.10.0             # automatic absent marking after shift ends

# ── Export ────────────────────────────────────────────────────────────────────
pandas>=2.0.0                   # for CSV export helpers

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0